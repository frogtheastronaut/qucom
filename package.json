{
  "name": "qucom-sdk",
  "version": "0.1.8",
  "description": "QUCOM - Quantum Computing SDK in JS/WASM",
  "main": "js/index.js",
  "files": [
    "js",
    "pkg/**/*"
  ],
  "type": "module",
  "scripts": {
    "build": "wasm-pack build --target bundler",
    "preinstall": "rm -f pkg/.gitignore || true",
    "prepack": "rm -f pkg/.gitignore || true",
    "prepublishOnly": "rm -f pkg/.gitignore || true"
  },
  "keywords": [
    "wasm",
    "rust",
    "quantum",
    "sdk"
  ],
  "author": "Ethan Zhang",
  "license": "MIT"
}
